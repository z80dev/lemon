{
  "name": "lemon-browser-node",
  "version": "0.1.0",
  "private": true,
  "description": "Browser node for Lemon (managed Chrome profile via CDP + Playwright)",
  "type": "module",
  "main": "dist/index.js",
  "bin": {
    "lemon-browser-node": "dist/cli.js"
  },
  "scripts": {
    "build": "tsup src/cli.ts src/index.ts src/local-driver.ts --format esm --clean",
    "dev": "tsup src/cli.ts src/index.ts src/local-driver.ts --format esm --watch",
    "start": "node dist/cli.js",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "playwright-core": "1.58.1",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@eslint/js": "^9.39.1",
    "@types/node": "^24.0.0",
    "@types/ws": "^8.18.0",
    "eslint": "^9.39.1",
    "globals": "^16.5.0",
    "tsup": "^8.0.0",
    "typescript": "^5.0.0",
    "typescript-eslint": "^8.46.4",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
