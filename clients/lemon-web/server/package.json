{
  "name": "@lemon-web/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "dev": "tsx src/index.ts",
    "build": "tsup src/index.ts --format esm --clean",
    "start": "node dist/index.js",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage"
  },
  "dependencies": {
    "@lemon-web/shared": "file:../shared",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "@vitest/coverage-v8": "^4.0.18",
    "@types/ws": "^8.18.1",
    "tsup": "^8.0.0",
    "tsx": "^4.19.2",
    "typescript": "^5.0.0",
    "vitest": "^4.0.18"
  }
}
