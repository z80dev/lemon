console.log("This server has been replaced by the native LemonGateway WebSocket transport.");
console.log("Please run the Elixir app (mix phx.server or iex -S mix) and use the Web UI.");
process.exit(0);
