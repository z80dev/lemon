# lemon_browser

Browser capability driver for Lemon.

Owns `LemonBrowser.LocalServer`, `LemonBrowser.Artifacts`, and
`LemonBrowser.RoutePolicy`. It depends only on `lemon_core` plus JSON support
and is consumed by browser tools and control-plane browser methods.
