[project]
name = "session-analyzer"
version = "0.1.0"
description = "Mine AI session data for recurring skill/subagent/tool patterns"
requires-python = ">=3.12"
dependencies = [
    "orjson>=3.10",
    "polars>=1.0",
    "duckdb>=1.0",
    "rich>=13.0",
    "scikit-learn>=1.5",
    "click>=8.0",
]
